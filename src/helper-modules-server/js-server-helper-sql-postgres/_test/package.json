{
  "name": "js-server-helper-postgres-test",
  "description": "Test suite for js-server-helper-postgres",
  "version": "1.0.1",
  "main": "test.js",
  "private": true,
  "license": "MIT",
  "author": {
    "name": "sj00"
  },
  "dependencies": {
    "@superloomdev/js-.*": "1.0.0",
    "@superloomdev/js-.*": "1.0.0",
    "@superloomdev/js-.*": "1.0.0",
    "@superloomdev/js-.*": "1.0.0",
    "pg": "^8.20.0"
  },
  "scripts": {
    "pretest": "docker compose down -v --remove-orphans 2>/dev/null; docker compose up -d --wait && sleep 2",
    "test": "POSTGRES_HOST=localhost POSTGRES_PORT=5432 POSTGRES_DATABASE=test_db POSTGRES_USER=test_user POSTGRES_PASSWORD=test_pw node --test --test-timeout 120000 test.js",
    "posttest": "docker compose down -v --remove-orphans"
  }
}
