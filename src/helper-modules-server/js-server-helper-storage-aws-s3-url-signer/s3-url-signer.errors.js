'use strict';

/**
 * Error catalog for js-server-helper-storage-aws-s3-url-signer.
 * Operational errors returned via {success: false, error}.
 * Frozen to prevent accidental mutation.
 */

module.exports = Object.freeze({

  STORAGE_URL_GENERATION_FAILED: Object.freeze({
    type: 'STORAGE_URL_GENERATION_FAILED',
    message: 'Storage presigned URL generation failed'
  })

});
