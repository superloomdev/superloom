# MongoDB for logger-store-mongodb module testing (single-node replica set)
# Lifecycle managed by npm scripts: npm test starts -> tests -> stops and removes
# Ephemeral - data lost on container stop
# Port 27020 to avoid conflict with auth-store-mongodb (27018) and verify-store-mongodb (27019)

name: superloom-test-logger-store-mongodb

services:

  mongodb:
    image: mongo:8
    container_name: superloom-test-logger-store-mongodb
    command: ["--replSet", "rs0"]
    ports:
      - "127.0.0.1:27020:27017"
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "try { rs.status() } catch(e) { rs.initiate({ _id: 'rs0', members: [{ _id: 0, host: 'localhost:27017' }] }) }; if (!db.hello().isWritablePrimary) quit(1)"]
      interval: 2s
      timeout: 5s
      retries: 15
      start_period: 5s
