# DynamoDB Local for auth-store-dynamodb module testing
# Lifecycle managed by npm scripts: npm test starts -> tests -> stops and removes
# Ephemeral - data lost on container stop
# Port 8001 to avoid conflict with js-server-helper-nosql-aws-dynamodb test on 8000

name: superloom-test-auth-store-dynamodb

services:

  dynamodb:
    image: amazon/dynamodb-local:latest
    container_name: superloom-test-auth-store-dynamodb
    ports:
      - "127.0.0.1:8001:8000"
    command: ["-jar", "DynamoDBLocal.jar", "-sharedDb"]
    healthcheck:
      test: ["CMD-SHELL", "curl -s -o /dev/null http://localhost:8000 2>/dev/null; [ $? -ne 7 ]"]
      interval: 2s
      timeout: 3s
      retries: 15
      start_period: 5s
