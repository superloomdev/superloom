// Info: Client Model Extensions - exports all client-side model extensions
// Each entity's client model extends the base model with client-specific fields
'use strict';

module.exports = {
  Survey: require('./survey')
};
